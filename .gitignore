# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.coverage
htmlcov/

# Local DB
*.db
*.sqlite
*.sqlite3
store_intelligence.db

# Videos / datasets / outputs
data/clips/
data/outputs/
data/uploads/
*.mp4
*.webm
*.avi
*.mov
*.mkv
*.zip

# Generated files
data/generated_events.jsonl
logs/
submission_build/

# NOTE: data/pos_transactions.csv is the DE-IDENTIFIED real Brigade POS export
# (all customer + employee personal fields stripped — see scripts/deidentify note
# in README). It is intentionally committed so the real-data demo works out of
# the box. Never commit the raw export that still contains names/phone numbers.

# Model weights
*.pt
yolo*.pt
*.onnx
*.engine

# Env
.env
